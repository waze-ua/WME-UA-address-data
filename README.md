# WME UA-address data

The Greasy Fork plugin for Waze Map Editor.

Shows polygons and addresses on a map in different locations.

## Development

```bash
npm install
npm run build
npm run watch   # rebuild on changes
```

Source is in `src/`, build output is `dist/WME_UA_address_data.user.js`.
